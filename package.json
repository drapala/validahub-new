{
  "name": "validahub",
  "version": "0.1.0",
  "private": true,
  "description": "ValidaHub — plataforma de validação e correção de CSV/Sheets",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rimraf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}